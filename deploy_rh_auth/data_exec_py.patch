# ITEM 3 (revised) — agents/data_exec.py: the alert hook the RobinhoodBroker fires on a
# 401 down/recovered TRANSITION. Reuses the existing _safety_push (telegram) + log_event
# (audit) plumbing, exactly like _handle_stale_snapshot. RESTART-gated (batch).
#
# De-dup is upstream: the broker only calls this on a state TRANSITION (latch), so this is
# an alert, not a per-call storm.

=== new method on DataExecAgent (place near _handle_stale_snapshot, ~line 332) ===
+    async def _on_rh_auth_change(self, down: bool, info: dict) -> None:
+        """RobinhoodBroker auth-state hook (ITEM 3). Fired ONCE per down/recovered
+        transition. `down=True` => auth failed AND the in-process reload could not recover
+        (pickle file also stale) => operator action needed."""
+        kind = "rh_auth_failed" if down else "rh_auth_recovered"
+        ts = iso(now_utc())
+        payload = {
+            "reason": info.get("reason"),
+            "since": info.get("since"),
+            "last_good": info.get("last_good"),
+            "accounts": ["680725082", "461391328", "934310442", "116637293063"],
+            "ts": ts,
+        }
+        audit_id = self.logger.log_event(actor="data_exec", kind=kind, payload=payload)
+        # Re-read confirmation (mirror _handle_stale_snapshot).
+        if audit_id is not None:
+            try:
+                with db.connect(self.logger.db_url) as conn:
+                    if conn.execute("SELECT 1 FROM audit_event WHERE id=?", (audit_id,)).fetchone() is None:
+                        log.error("%s audit_id=%s could NOT be re-read — possible silent drop", kind, audit_id)
+            except Exception as e:
+                log.warning("audit re-read after %s failed: %s", kind, e)
+        if down:
+            text = ("RH SESSION DOWN — Robinhood auth failing (401). NO new entries AND "
+                    "NO exits on live positions; broker-wide (PEAD / PMCC / IRA / joint). "
+                    f"since {info.get('since')}. In-process reload could not recover (pickle "
+                    "likely stale) — approve a refresh: dashboard 'Refresh RH pickle' button.")
+        else:
+            text = f"RH session RECOVERED (auth restored). last good {info.get('last_good')}."
+        await self._safety_push(
+            text, audit_path="safety_alert",
+            audit_context={"division": "robinhood", "kind": kind},
+        )

# NOTE: data_exec.snapshot() (line 96-97) is left unchanged — the broker self-heals and
# never raises for auth, so callers (manage/scan/pmcc/dashboard) are unaffected.

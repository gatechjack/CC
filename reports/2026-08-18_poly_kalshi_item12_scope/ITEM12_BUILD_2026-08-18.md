# Item 1 (fail-closed flip) + Item 2 (quote fix) — BUILT (branch-only, NOT deployed)

Build session 2026-08-18. Live loop untouched (ARMED, PID 775659). Nothing deployed — deploy waits
for MACE's deploy to fully finish + settle (do not race two deploys on one prod-live / one shared engine).

## STEP 0 — coordination pre-check vs concurrent MACE build: CLEAN, NO OVERLAP
- **Worktree isolation:** NEW dedicated worktree `cc-poly-kalshi-item12-build-2026-08-18-wt`,
  branch `poly-kalshi-item12-build-2026-08-18`, based on `f696c8e` (item12-scope tip, Item 1 already built).
  MACE's worktree is a different dir (`cc-mace-band-widen-wt`) — no sharing.
- **Branch base:** `f696c8e` contains **zero** in-flight MACE commits (checked `653a649`, `17ad957`,
  `mace-band-widen-2026-08-18`, `mace-weekly-rungs-5-2026-08-18` — none are ancestors). The two lineages
  diverged back at `2528aaa` (08-14). MACE is building on `mace-band-widen-2026-08-18`, not my branch.
- **Shared-file check:** MACE's in-flight (uncommitted) changeset = `config/mace.yaml` +
  `trading_corp/mace/config.py` only. This work touches `poly_kalshi_executor.py`, `mlb_poly_kalshi_match.py`,
  `brokers/kalshi.py` (+ tests). **Intersection: EMPTY.** `brokers/kalshi.py` last touched 2026-07-01
  (`41ad9d6`), stable, not in MACE's set.
- **Verdict: NO MACE OVERLAP — safe to build.** (Deploy is still sequenced after MACE.)

## STEP 1 — Item 1: [G-conflict] fail-CLOSED on DB/lookup error (`7c17edf`)
Jack ruled fail-CLOSED. Changed ONLY the DB-error path in `_opposite_side_on_game`: a lookup exception
now returns `{'status':'skip_gate_error'}` → the placement is **SKIPPED** (was: fail-open/allow).
`submit()` branches on the returned status. **Unparseable ticker still fails OPEN** (returns None, not an
exception → allowed; idempotency + slippage backstop) — that path deliberately unchanged.
- Tests: `test_gconflict_db_error_fails_closed` (monkeypatch the lookup to raise → `skip_gate_error`,
  `_orders_today==0`, no key burned) + `test_gconflict_unparseable_still_fails_open_after_flip`.

## STEP 2 — Item 2: KalshiBroker.quote() → MarketModel top-of-book (`332151e`)
Fix B (ratified). `brokers/kalshi.py` `quote()` (276-306) now reads `market.yes_bid_dollars` /
`yes_ask_dollars` (the proven `_pk_quote_fn` source) and returns their mid; the broken
`get_orderbook().yes_bids/yes_asks` parse is dropped. New `_dollar_price` helper (parses a dollar string
to a float in (0,1)); removed the now-dead `_best_price` (wrong-model). No extra API call — `quote()`
already fetched `market`.
- **brokers/kalshi.py is NOT byte-locked** (the 3 locked = kalshi_copy_trader / sports_team_mapping /
  kalshi_live). It IS shared. Caller safety (re-confirmed):
  - `kalshi_copy_trader.py:619-623` — fallback exit price, guarded `if yes_mid>0`, resolution-first
    unchanged → 0.0→real mid = **helped, can't break**. (kalshi_copy_trader test suite green.)
  - `portfolio.py:49` — Kalshi position mark 0→real → **helped**.
  - slippage guard `_pk_quote_fn` (main.py) — separate MarketModel path, **never calls quote()** → untouched.
- Tests: `test_kalshi_broker_quote.py` (full-book→mid 0.31 / one-sided / settled|missing→0.0 / stub→0.0 /
  get_market error→0.0 / `_dollar_price`), + `test_poly_kalshi_marks.py` e2e (a REAL KalshiBroker with the
  fix now marks the position: `marked=1`, was historically all `quote_miss`).

## Verification
- **Byte-locked:** `git diff --name-only 827bea9 -- <3 locked>` = empty (all byte-unchanged).
- **Full item12 changeset** (vs phase2a base `827bea9`): 3 runtime + 3 test files only.
- **Suite:** poly_kalshi (executor/copy_trader/marks/reconciliation/live_view/boot_refresh) + matcher +
  broker-quote + kalshi_copy_trader = **146 pass**, zero regressions.

## Deploy plan (SEPARATE operator-run step, AFTER MACE finishes + settles)
Two independent deploys (do not merge): Item 1 = `poly_kalshi_executor.py` + `mlb_poly_kalshi_match.py`;
Item 2 = `brokers/kalshi.py`. File-overwrite per the prod-live-advance rule (LF-md5 vs box), restart the
shared engine (arms), advance prod-live same session. Sequence AFTER MACE's deploy so two restarts/two
prod-live advances don't race. Post-deploy confirm: Item 1 → a live opposite-side signal logs
`skip_conflict`; Item 2 → `poly_kalshi mark tick` shows `marked>0`.

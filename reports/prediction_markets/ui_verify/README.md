# PM UI rewrite -- live page verification (2026-09-02)

Generated by `reports/prediction_markets/ui_verify_harness.py` (offline TestClient render; temp DB with a
representative journal; REAL Kalshi marks fetched live; synthetic feed states because today's games are
pre-game at the run hour). 11/11 assertions pass.

States rendered (standalone HTML with inlined CSS + PNG screenshots):
- `01_active_live_mixed` -- Active tab: SD@CIN LIVE (diamond, bases 1st+3rd, TOP 6, B/S/O pips, linescore,
  last play; ML slot valued at the REAL bid $2.90 = 5 x 0.58; TOT/SPR 'no mark' -- those synthetic strike
  tickers are not in the live book, the honest degrade even while other tickers have real marks) + MIL@CHC
  FINAL mixed (ML won $5.00, SPR lost $0.00, TOT live 'no mark').
- `02_complete_tab` -- Complete tab: the all-settled game (STL@LAD).
- `03_feed_unavailable` -- empty slate: FEED UNAVAILABLE card, count 'unavailable' (never a fabricated 0-0).
- `04_no_mark` -- slate present, marks empty: every open slot shows 'no mark' (never $0.00 / cost-as-value).

Honesty checks asserted: value labelled as BID; cost basis distinct from current value; opposed close shows
'-- not booked' + OPPOSED status; unknown whale shows its wallet; feed-unavailable renders nothing feed-derived.

Reproduce: `PYTHONPATH=<worktree> <venv>/python reports/prediction_markets/ui_verify_harness.py`

# T1 RUNG 2 — one manual FULL-BOOK poller run — 2026-08-27

**Authorization (Jack):** "T1 RUNG 2 — ONE MANUAL POLLER RUN ON THE FULL BOOK. AUTHORIZED … a SINGLE poll_pinned
run ONLY." No adjudicate/rollup/cadence/deploy/restart/prod-live. Ran **18:53 UTC** (clear of 03:20; nothing else
wrote the PM DB). Invocation: `pm_cli paper-poll` (normal, no params). **Exit 0, runtime 4.82 s.**

## Pre-conditions (matched) + rollback
schema 9 · grace 259200 · pm_paper_trade **107 (104 open / 1 pending / 2 closed)** · pm_watchlist **114/92/22** ·
pcs **6** · /farm 200/**130199** · engine **676** / pm_web **24808** · UTC 18:52. **Gate-1 backup (rollback):**
`~/pm_t1_rung2_poll_dbbackup_20260827T185228Z.db` (sha `02bf6b6b…`, `integrity_check=ok`).

## Answers

**(a) ★ Completeness per whale — all 14 confirmed complete; the fail-closed gate did NOT fire.** `incomplete=0`.
Every whale returned a terminal (short) final page, so `book.complete=True` for all 14; no whale was left un-polled.
The first live exercise of the fail-closed path had nothing to catch (clean, as hoped).

**(b) Positions retrieved — 2746 vs the ~1400 the old first-page-only code saw (~1.96×).** Per whale (positions,
pages): BetMechanic **1311 / 3pg**, SDTrading **515 / 2pg**, then 210, 158, 123, 112, 74, 67, 58, 54, 36, 26, 2, 0 —
all 1 page. **2 multi-page whales**, 17 requests, **runtime 4.82 s** (was 2.94 s on half the book). Matches the
read-only cost probe (2745; +1 = normal churn).

**(c) New paper trades — 14, up from last run's 5.** By category: **mlb 12 (one pair), ufc 1, atp 1** — 3 pairs,
3 categories, **0 unknown**. ★ The **12 mlb captures are one whale, SDTrading** — game-line positions that sat below
the old top-100 page and were **never captured before**; this is the completeness payoff in one number. **`atp`
(tennis) is a NEW category surfacing** — a match line the capped poll had missed. (Last run: ufc·2/mlb·2/cs2·1.)

**(d) ★ The active gate under twice the data — HELD, ZERO leakage.** trades in deactivated pairs **94 → 94** · new
trades in deactivated pairs **0** · any deactivated trade touched **0** · roster polled this run **92**, of them in
deactivated pairs **0**. The `active=1` gate held on the full 2746-position book exactly as it did on half of it.

**(e) open → pending transitions — 0.** Nothing vanished: the active-pair open trades (incl. the newly captured) are
all still genuinely-open in the whales' current books, and the 1 pre-existing pending (mlb SF@ATL) is `status !=
'open'` so the poller doesn't re-touch it. Pending stays 1.

**(f) Before → after by status:** total **107 → 121**; open **104 → 118**; pending **1 → 1**; closed **2 → 2**.
(+14 new opens, 0 vanished, 0 newly closed.)

**(g) Categorization under 2× data — still ~0 tier-2 miss on tracked categories.** 152 skipped (≈2× last run's 90),
**all derived `unknown`**. Classification: **139 futures/novelty**, 11 other-novelty, and **2 `-vs-` "candidates" =
`floyd-mayweather-vs-manny-pacquiao` (boxing)** — boxing is **not one of the 15 tracked tiles**, so it is correctly
out-of-scope `unknown`, NOT a tier-2 miss (a tier-2 miss is a *trackable tile-category* line wrongly dropped). That
`atp` was captured (not skipped) confirms tennis match lines categorize correctly. **Zero tracked-category game lines
were lost even at twice the data** — the ~0 tier-2 finding holds.

**(h) Errors / rate-limiting — 0.** No errors, no 429s at 17 requests / 4.82 s.

## Post-verify
schema **9** · grace **259200** · pm_watchlist **114/92/22** unchanged · pm_paper_category_stats **still 6 rows** —
**now STALE relative to the 14 new trades** (they under-count the new opens and omit the new atp pair); this is
**expected and correct until the next rollup** (unauthorized). /farm **200, 130199 — byte-identical**. **Why it did
NOT move:** the `/farm` **landing** (category=None) renders generic poll status ("polled" ×93) + pcs-sourced stats
(the stale 6 rows); it does **not** render per-pair *live* open counts in a byte-varying way. The DB confirms the new
opens (SDTrading/mlb op.n_open **14**, 4751346/ufc **3**, MadeiraIsland/atp **1**) — they surface on the category
**detail** pages and after the rollup, not on the landing. engine **676** / pm_web **24808** PIDs unchanged.

## STOP conditions — none triggered
No deactivated pair polled/traded (0) · pm_watchlist unmoved · incomplete gate fired on **0** whales · 0 errors/429s.
**Clean run.**

## What this run established
- The completeness fix works end-to-end on live: **2746 vs ~1400** positions, all 14 books confirmed complete, and
  **the previously-hidden in-category positions are now captured** (SDTrading's 12 mlb + a new atp pair).
- The **honest per-poll funnel yield** on the full book: **24 in-pinned-category genuinely-open positions**
  (14 newly captured + 10 existing touched/added), across 3 categories this instant — up from 10 in-category on the
  capped poll.
- The safety properties (active gate, fail-closed completeness, categorization) all hold under 2× the data.

## Still unauthorized
The **rollup** (which would refresh the now-stale pcs 6 rows to reflect the 14 new trades), the **cadence install**,
and **advancing origin/prod-live** all remain unauthorized.

Runner: `cc\pm_t1_poll_pre.sh`, `cc\pm_t1_poll_run.sh`, `cc\pm_t1_farm_check.sh`.

# Kalshi "geo-403" — SCOPED, and the standing diagnosis is WRONG (2026-08-24 17:10 UTC)

Read-only, unauthenticated probe (`pm_kalshi_403_scope.ps1`), engine-PID bracketed unchanged (850993).

## ★★ HEADLINE: the live failure is a **401 authentication_error**, NOT the recorded 403 geo-block

`poly_kalshi_mlb` is **still armed and looping** — every ~7s the journal shows:
```
ERROR trading_corp.main: poly_kalshi loop cycle failed:
  401: authentication_error (authentication_error) [POST /portfolio/events/orders]
```
(via `poly_kalshi_executor.submit`). It is **NOT** the recorded `403 Trading not allowed in WASHINGTON on Sports`. The engine's Kalshi **key is being rejected** — an AUTH failure, not a geolocation block. No orders fill (401 rejects before placement); no money moves.

## Proof it is NOT geo (unauthenticated reads all 200, including Sports)
Egress IP = **168.62.60.79** (unchanged). Every unauthenticated read returned **200** with real data:

| host | request | result |
|---|---|---|
| external-api.kalshi.com | `/markets?limit=2` | **200** (markets incl. EPL/MLB/LaLiga) |
| external-api.kalshi.com | `/events?series_ticker=KXMLBGAME` | **200** — MLB games, `"category":"Sports"`, `"competition":"Pro Baseball"` |
| api.elections.kalshi.com | `/markets?limit=2` | **200** |
| api.elections.kalshi.com | `/series?category=Sports` | **200** (sports series listing) |
| api.elections.kalshi.com | `/series?category=Economics` | **200** |

If this egress were geo-blocked on Sports, reading Sports/MLB markets would 403. It does not. **The geo-403 diagnosis is superseded for the current state; the live block is an authentication failure on the order path.**

## Task 1a — ANSWERED: reads/listings are NOT blocked
Market listings, events, and series (Sports and non-Sports, both hosts) are readable unauthenticated. The mark poller / listing / quote paths would function — they do not depend on the failing order-auth. (This also resolves the 08-20 open question: the poller could run.)

## Task 3 — demo host IS reachable
`external-api.demo.kalshi.co/trade-api/v2/markets` → **200** (returns demo markets incl. `KXWNBAGAME`). Demo is not geo-blocked from this egress. (Demo still needs a separate keypair + funded account we do not hold; market universe unverifiable without them; it answers build/test only, none of the prod-listing questions.)

## What this means (pending the authenticated key test)
The failure is AUTH, not geo — so a **valid key should fix it**, exactly the case flagged: "if the new key works, the standing diagnosis needs correcting." The existing key is rejected (401). The **decisive next test** is the authenticated ladder with the existing vs the new `Kalshi-Karen-TWO` key (Runner 2) — does the existing key still 401, and does the new key authenticate? UNVERIFIED until run.

## Part 2 (Task 1b + 2) — key auth test: ALL THREE keys authenticate; the block is NOT a dead key either

Read-only `KalshiBroker` (no `place_order`), keys fetched by exact name via the VM Managed Identity, `get_balance` = the auth smoke, engine-PID bracketed unchanged (850993). Secrets never printed.

| key (KeyVault) | retrieved | `get_balance` (both hosts) | account (balance / portfolio) |
|---|---|---|---|
| `KALSHI` (original) | id+pem ✓ | **AUTH OK** + market read ACTIVE | $165.39 / $314.20 |
| `KALSHI-KAREN` (one) | id+pem ✓ | **AUTH OK** + market read ACTIVE | $493.51 / $17.29 |
| `Karen-TWO` (new) | id+pem ✓ | **AUTH OK** + market read ACTIVE | $493.51 / $17.29 |

**Findings:**
- **Jack's new `Karen-TWO` key WORKS** (authenticates + reads). It's a second valid key on the **same account** as `KALSHI-KAREN` (identical balance $493.51/$17.29). `KALSHI` (original) is a different account.
- **All three keys authenticate from the box egress on both hosts.** So the poly_kalshi `401 authentication_error` on `POST /portfolio/events/orders` is **NOT a dead/rejected key** (and Part 1 already showed it's **NOT geo** — reads 200). The **same credentials that authenticate here are what the engine holds, yet its order POST 401s.**
- **Therefore the 401 is ORDER-PATH-SPECIFIC** — reads (`get_balance` GET, `get_market`) succeed with these keys; only the order POST fails. It is neither geolocation nor an invalid credential.

**Open question (flag, do NOT fix — needs a follow-up diagnostic + Jack's call):** why does the order POST 401 while everything else works with the same keys? Candidates: (a) the engine holds a **stale in-memory key** from its 02:17 UTC startup that's since been rotated in the vault (the current vault values all work → an engine restart would reload them); (b) an **order-endpoint-specific signing/scope issue** in the rebuilt V2 `POST /portfolio/events/orders` path. Distinguishing them needs a scoped follow-up (compare the engine's loaded key to the current vault, and/or probe the order-path signing) — beyond "test the key." No restart / change taken.

## Part 3 (follow-up) — distinguishing (a) stale key vs (b) order-path defect

poly_kalshi_mlb signs with **KALSHI-KAREN** (`divisions.yaml secret_ref: kalshi_karen`; its OWN Karen-keyed `KalshiLiveBroker`; `auto_execute: true` = armed/live). That key authenticates fine (Part 2), so the engine holds *some* KALSHI-KAREN value and 401s only on the order POST.

**In-memory limit (UNVERIFIED by direct read):** the engine's loaded key lives in runtime `os.environ` (set by `_populate_from_keyvault` AFTER fork) — NOT in `/proc/<pid>/environ` (exec-time only). Reading it directly would need a debugger attached to the live trading engine (not read-only-safe). So a direct value/hash of process memory is unavailable.

**The read-only distinguisher (`pm_kalshi_keydiag`):** the live broker materializes its PEM to a `/tmp/kalshi_*.pem` tempfile for the process lifetime (`KalshiBroker.connect`). The diagnostic HASHES that tempfile and compares to the current vault value BY FINGERPRINT (truncated sha256; value never printed):
- **match** → engine holds the current key → **(b) ORDER-PATH defect** (restart pointless; fix is in the V2 POST signing).
- **differ** → engine holds a stale/rotated key → **(a) STALE key** (a deliberate restart reloads current vault).
- If `PrivateTmp` isolates the tempfile (unreadable), it FALLS BACK to vault KALSHI-KAREN `updated_on` vs engine start, plus a `.env`-override check (a `.env` value would make a restart reload the SAME stale value) — UNVERIFIED-direct, timestamp as proxy.

## Two Kalshi accounts already exist (record for the P3 (ACCOUNT, CATEGORY) sub-division model)
- **KALSHI** (original): a distinct account — $165.39 / $314.20.
- **KALSHI-KAREN** + **Karen-TWO**: the SAME account — $493.51 / $17.29. Karen-TWO is a **second valid key on the KALSHI-KAREN account, NOT a third account.**

## Strategic note
The standing posture — "geo-403, wait for Kalshi, plan a whole-platform VM geo-migration" — rested on the geo inference, which two independent probes now contradict (reads all 200; all keys authenticate). **The VM-migration plan is NOT deleted, but its geo-403 justification is VOID** — if it is ever revived, its rationale must be re-established on its own merits. Jack is not waiting on Kalshi.

## Part 4 (result, 2026-08-24 18:24Z) — (a) STALE KEY, via the timestamp fallback (direct hash UNVERIFIED)

Engine 850993 unchanged. **poly_kalshi still armed + 401-looping** (`18:24:19 ... poly_kalshi loop cycle failed: 401 authentication_error [POST /portfolio/events/orders]`; body detail **`NOT_FOUND`** — the presented key is not found, consistent with a rotated-away credential). Expected: the vault write did not reach the running engine.

**★ HONEST LIMIT — the direct fingerprint did NOT read the engine's real key. `PrivateTmp=yes`**, so poly_kalshi's live-broker KALSHI-KAREN tempfile is inside the engine's private `/tmp` namespace (root-owned, unreadable to azureuser; the private-path glob found nothing). The `/tmp/kalshi_*.pem` the runner COULD read were **stale Aug-1 leftovers matching `KALSHI` (the original $165 account), NOT poly_kalshi's KALSHI-KAREN** — plus two root-owned Aug-24 tempfiles (unreadable, not the engine's — it runs as azureuser). So the runner's "DIFFER" was computed off the wrong files: **the direct value hash is UNVERIFIED**, not a proof.

**★ THE FALLBACK IS DECISIVE → (a):**
- **Vault `KALSHI-KAREN` `updated_on = 2026-08-24 17:15:33Z`, AFTER the engine start `2026-08-22 02:17:04Z`.** The value changed ~40h after the engine loaded it → the engine holds the pre-update value → its loaded KALSHI-KAREN differs from current vault → **(a) stale.**
- **`.env` absent** → the engine loads KALSHI-KAREN from KeyVault (no `.env` override) → **a restart reloads the current vault value → a restart IS the fix** (not a gamble).
- **Jack's write confirmed:** `KALSHI-KAREN` fingerprint `89da3fe2df34f228` **==** `Karen-TWO` `89da3fe2df34f228` → he copied the working Karen-TWO value into the KALSHI-KAREN name (not a third account/key). Write landed (recent `updated_on`).
- `KALSHI` (original) is unchanged (`updated_on 2026-05-10`), the account the stale leftovers belong to.

**CONCLUSION: (a) stale key. The engine holds the pre-17:15 KALSHI-KAREN value; the vault now holds the working one; `.env` absent → a deliberate restart reloads it and clears the 401.** Restart is Jack's call + timing (blips every division). NO restart taken.

### Restart scope (recommend, do NOT execute) — for Jack's decision
- **Blast radius:** `systemctl restart trading-corp.service` blips **every** division (one engine process): MACE, PEAD, bitunix (+SFP), robinhood_pead, poly_kalshi, kalshi_* — all reconnect.
- **poly_kalshi:** ~2-min boot blind spot on reconnect; the P1.5-style off-hours catch-up guard gates missed slots.
- **MACE timing:** manage loop runs ~09:35–15:55 ET, entry window 15:45–15:58 ET. A restart at 18:24Z (~14:24 ET) lands inside the manage window → cleaner to restart **after 15:55 ET / market close**, Jack's call.
- **Verify immediately after:** engine MainPID changed; `journalctl -u trading-corp.service` shows **no** new `poly_kalshi loop cycle failed: 401` and ideally a clean cycle / successful order; the other divisions boot (no new errors); equity tiles render.
- **Confirm the 401 actually cleared:** the journal is the signal — the 401 loop stops. (Re-running this diag won't re-read the key, PrivateTmp-hidden; the journal clearing is the proof.)

*Read-only. No orders, no code, no deploy, no restart. Engine/Caddy/Authelia/legacy untouched. Runners banked.*

# Kalshi "geo-403" — SCOPED, and the standing diagnosis is WRONG (2026-08-24 17:10 UTC)

Read-only, unauthenticated probe (`pm_kalshi_403_scope.ps1`), engine-PID bracketed unchanged (850993).

## ★★ HEADLINE: the live failure is a **401 authentication_error**, NOT the recorded 403 geo-block

`poly_kalshi_mlb` is **still armed and looping** — every ~7s the journal shows:
```
ERROR trading_corp.main: poly_kalshi loop cycle failed:
  401: authentication_error (authentication_error) [POST /portfolio/events/orders]
```
(via `poly_kalshi_executor.submit`). It is **NOT** the recorded `403 Trading not allowed in WASHINGTON on Sports`. The engine's Kalshi **key is being rejected** — an AUTH failure, not a geolocation block. No orders fill (401 rejects before placement); no money moves.

## Proof it is NOT geo (unauthenticated reads all 200, including Sports)
Egress IP = **168.62.60.79** (unchanged). Every unauthenticated read returned **200** with real data:

| host | request | result |
|---|---|---|
| external-api.kalshi.com | `/markets?limit=2` | **200** (markets incl. EPL/MLB/LaLiga) |
| external-api.kalshi.com | `/events?series_ticker=KXMLBGAME` | **200** — MLB games, `"category":"Sports"`, `"competition":"Pro Baseball"` |
| api.elections.kalshi.com | `/markets?limit=2` | **200** |
| api.elections.kalshi.com | `/series?category=Sports` | **200** (sports series listing) |
| api.elections.kalshi.com | `/series?category=Economics` | **200** |

If this egress were geo-blocked on Sports, reading Sports/MLB markets would 403. It does not. **The geo-403 diagnosis is superseded for the current state; the live block is an authentication failure on the order path.**

## Task 1a — ANSWERED: reads/listings are NOT blocked
Market listings, events, and series (Sports and non-Sports, both hosts) are readable unauthenticated. The mark poller / listing / quote paths would function — they do not depend on the failing order-auth. (This also resolves the 08-20 open question: the poller could run.)

## Task 3 — demo host IS reachable
`external-api.demo.kalshi.co/trade-api/v2/markets` → **200** (returns demo markets incl. `KXWNBAGAME`). Demo is not geo-blocked from this egress. (Demo still needs a separate keypair + funded account we do not hold; market universe unverifiable without them; it answers build/test only, none of the prod-listing questions.)

## What this means (pending the authenticated key test)
The failure is AUTH, not geo — so a **valid key should fix it**, exactly the case flagged: "if the new key works, the standing diagnosis needs correcting." The existing key is rejected (401). The **decisive next test** is the authenticated ladder with the existing vs the new `Kalshi-Karen-TWO` key (Runner 2) — does the existing key still 401, and does the new key authenticate? UNVERIFIED until run.

## Part 2 (Task 1b + 2) — key auth test: ALL THREE keys authenticate; the block is NOT a dead key either

Read-only `KalshiBroker` (no `place_order`), keys fetched by exact name via the VM Managed Identity, `get_balance` = the auth smoke, engine-PID bracketed unchanged (850993). Secrets never printed.

| key (KeyVault) | retrieved | `get_balance` (both hosts) | account (balance / portfolio) |
|---|---|---|---|
| `KALSHI` (original) | id+pem ✓ | **AUTH OK** + market read ACTIVE | $165.39 / $314.20 |
| `KALSHI-KAREN` (one) | id+pem ✓ | **AUTH OK** + market read ACTIVE | $493.51 / $17.29 |
| `Karen-TWO` (new) | id+pem ✓ | **AUTH OK** + market read ACTIVE | $493.51 / $17.29 |

**Findings:**
- **Jack's new `Karen-TWO` key WORKS** (authenticates + reads). It's a second valid key on the **same account** as `KALSHI-KAREN` (identical balance $493.51/$17.29). `KALSHI` (original) is a different account.
- **All three keys authenticate from the box egress on both hosts.** So the poly_kalshi `401 authentication_error` on `POST /portfolio/events/orders` is **NOT a dead/rejected key** (and Part 1 already showed it's **NOT geo** — reads 200). The **same credentials that authenticate here are what the engine holds, yet its order POST 401s.**
- **Therefore the 401 is ORDER-PATH-SPECIFIC** — reads (`get_balance` GET, `get_market`) succeed with these keys; only the order POST fails. It is neither geolocation nor an invalid credential.

**Open question (flag, do NOT fix — needs a follow-up diagnostic + Jack's call):** why does the order POST 401 while everything else works with the same keys? Candidates: (a) the engine holds a **stale in-memory key** from its 02:17 UTC startup that's since been rotated in the vault (the current vault values all work → an engine restart would reload them); (b) an **order-endpoint-specific signing/scope issue** in the rebuilt V2 `POST /portfolio/events/orders` path. Distinguishing them needs a scoped follow-up (compare the engine's loaded key to the current vault, and/or probe the order-path signing) — beyond "test the key." No restart / change taken.

*Read-only. No orders, no code, no deploy, no restart. Engine/Caddy/Authelia/legacy untouched. Runners banked.*

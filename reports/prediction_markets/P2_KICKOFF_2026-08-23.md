# Prediction Markets — P2 BUILD KICKOFF (2026-08-23)

**Status:** Phase-2 build STARTED. Branch cut, nothing else built/deployed/mutated yet. Engine untouched.
**Spec:** `P2_PLAN.md` (BOARD-APPROVED FINAL 2026-08-23) is the executable spec. This note is the build agent's
kickoff record — the gate resolution + the load-bearing rulings that were NOT obvious from the plan alone.
Read `TRANSITION_TO_P2_BUILD_AGENT.md` first, then `P2_PLAN.md`, then this.

---

## Branch model — READ THIS, it is the trap the P1 docs set for you

`prediction-markets-p2-2026-08-23` was cut off **durable `prediction-markets` @ `a95d558`** and merges back into
durable (fast-forward). Push to origin from the first commit; keep pushed. **NO merge to `main`** until the single
P3 cutover. `prod-live` advances for DEPLOYED artifacts only.

**BASE (durable a95d558) and DEPLOY-ANCHOR (prod-live 86fb433) are DISTINCT ROLES. Every governing doc that says
"cut off the prod-live tip" was written when they were the SAME commit and is now WRONG if read literally.**

- **DURABLE `prediction-markets` @ `a95d558` = the BRANCH BASE.** Carries the P1 package + `pm_cli` + the full
  `tests/prediction_markets/` suite + fixtures + all `reports/` specs. Phase work cuts from here, FFs back here.
- **`prod-live` @ `86fb433` (Jack-confirmed 2026-08-23) = the DEPLOY-ADVANCE ANCHOR ONLY.** Carries the P1
  package + `pm_cli` but **NO tests suite and NO `reports/` docs**. Deployed P2 artifacts land on it via
  path-checkout at deploy time (exactly as P1 did). **It is NOT a development base.**

Why this matters, proven by topology (2026-08-23): `prod-live` and durable have **diverged** — merge-base is
`8d77a26` (the shared P1 branch-off base), and **neither is an ancestor of the other**. In P1, "prod-live tip"
and "durable base" were the SAME commit (`8d77a26`), so "cut off prod-live" was unambiguous then. It no longer is.
Branching off `prod-live` today would strand the build without the P1 test suite (which `P2_PLAN.md` §13 requires
stay green) and turn the merge back to durable into a cross-line merge instead of a fast-forward.

---

## Rulings carried into the build (Jack, 2026-08-23 gate)

These are the findings from the grounding report that Jack ruled on. Treat each as binding.

### (e5) — HARD REQUIREMENT: migration-004 caveat columns must be wired into rollup(), or they read ZERO forever
`stats.rollup()` writes `pm_category_stats` via `INSERT OR REPLACE ... (_STATS_COLS)` — it **rewrites the whole
row every run**. Therefore ANY column added by migration 004 that is NOT also added to `stats._STATS_COLS` **and**
computed in the rollup SELECT is reset to its column DEFAULT (0 / NULL) on every rollup. The new caveat columns
(`two_sided_pct`, `single_game_pct`, `n_condition_ids`, `n_two_sided`, `n_single_game`, `n_futures_like`,
`market_type_source`) and the one-sided companion (`pm_category_onesided_stats`) would silently read ZERO on the
product page, forever — a P1-class silent-wrong-data bug.
**REQUIREMENT:** the migration-004 change ships together with the `_STATS_COLS` + rollup-SELECT extension. The
CP1->CP2 checkpoint MUST run a rollup on real data and VERIFY `two_sided_pct` / `single_game_pct` / the one-sided
slice are POPULATED WITH REAL VALUES — not defaulted. **A zero that could be either real or defaulted is a FAIL
until proven which.**

### (e7) — PINNED NOW: pm_paper_trade.size_basis = FIXED CONTRACT/SHARE COUNT, not fixed dollars
`cost_basis = size_basis * entry_price` only parallels the external side's `cost_basis = total_bought(NOTIONAL) *
avg_price` if `size_basis` is a share/contract count. Fixed DOLLARS would make `cost_basis` mean something
different on the two halves of the same scoreboard — the exact definitional drift that caused the P1
notional-denominator bug. **The migration-005 schema comment must state `size_basis` = fixed contract/share count
so it cannot drift.** (OQ-1 "fixed unit for comparability"; ROI normalizes either way, but the columns' meaning
must match the external side.)

### (e3) — ANALYZE is a COPY of the engine's LLM audit/narration code, and KeyVault access is a PREMISE
`pm_web` has no engine imports, but the audit-building + narration lives in the engine web routes
(`routes.py ~3155-3376`). It must be **copied** into `pm_web` (reuse != coupling, allowed) — not "rewired". And a
second systemd unit reaching the Anthropic key via `KEY_VAULT_URI` + VM managed identity is a PREMISE, not a fact.
**Prove the PM unit can reach the key BEFORE wiring ANALYZE. If it cannot, STOP AND REPORT — do not improvise
credential handling.**

### (e4) — "Do NOT touch ingest.py" means ingest.py ONLY. pm_cli.py and rosters.py are in-bounds.
The roster-source flip (Ruling B) does NOT live in `ingest.py` (which never reads the roster). It lives in
`pm_cli._seed_wallets` + a new `pm_roster` reader in `rosters.py`. Both are PM-package files the plan already
extends (it adds `migrate-roster` / `paper-poll` subcommands to `pm_cli`). "No edits" = engine files + `ingest.py`;
it does NOT cover the rest of the PM package.

### (e6) — P2 ships TWO writers, not three. (e2) — /activity fields verified present.
`P2_PLAN.md` §3.2 lists three cadences; the third ("live marks", seconds) is P3 (plan-the-slot-only). P2 ships the
weekly full refresh + the ~30-min paper-entry poll (+ pm_web's small synchronous writes). `/activity` capture is
real: `PolymarketDataAPIClient.fetch_activity` + `ActivityRow` carry `timestamp` (entry ts), `side` (BUY/SELL),
`price`, `size`/`usdc_size`, `condition_id`, `outcome_index`. CP4 verifies polling behavior + the 5000-row
`/activity` truncation ceiling, not a missing field.

---

## Build checkpoints (HALT-AND-REPORT; never a chain point)
1. **CP1** — migration 004 applied to the LIVE P1 DB: `schema_version -> 7`, existing ~28.3k rows intact, re-run
   no-op, ownership azureuser. Then run one rollup so the caveat columns populate. HALT.
2. **CP2** — first page rendering REAL data: caveat columns + flag set + drill-throughs match `format_report` on
   the same rows (product/CLI parity); (e5) populated-with-real-values check. HALT.
3. **CP3** — first search run: inspect `pm_search_run` + found candidates + one targeted `--only-wallets` backfill
   made rankable, before bulk/concurrent. HALT.
4. **CP4** — first paper trade captured: inspect one `pm_paper_trade` row (entry_ts/side/size/cost_basis, §3A
   parity) from a real `/activity` fill, before the poll runs across the farm. HALT.

## Hard constraints + standing principles (in force; full text in TRANSITION §6/§7)
Additive only; zero engine-file edits; no engine restart (pm_web is a separate unit, never touches
`trading-corp.service`); never write legacy `trading_corp.db`; do NOT touch `ingest.py`; no auth tables; do NOT
edit Caddy/Authelia; no `main` merge; all box ops via the `.ps1` runner pattern; **azureuser, NEVER root**
(GOTCHA-1); never reword a blocked command. BIAS DOWN NEVER UP; verify empirically, never narrate; caveats travel
with the number (on the product page); every aggregate drills to its rows; read cost-ROI + avg_win_price + the
one-sided slice, never the composite score alone. Live-money status leads every report; paste raw output; show
failures as failures; say UNVERIFIED when unverified. Expect this plan to be wrong elsewhere too.

---

## Immediate sequence
1. [DONE] Cut `prediction-markets-p2-2026-08-23` off durable `a95d558`; push from first commit.
2. [DONE] This kickoff note (first artifact).
3. Run the P1 test baseline green (local, offline). Paste raw output.
4. Re-verify live box state via a read-only `.ps1` runner (cron, row counts, DB ownership, port 8081 free,
   weekly-slot clearance) — do not trust remembered/doc numbers, including the confirmed ones.
5. CP1. HALT.

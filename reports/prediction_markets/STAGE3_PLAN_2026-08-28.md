# Stage 3 (Money Layer) — PLAN + R0 findings (2026-08-28)

**Status: ACCEPTED (Jack, 2026-08-28). Rulings DECIDED (§9). R0 + R1 schema + R2 matcher + R3 LIVE-list EXECUTED (§12–§15).**
**★ UPDATE 2026-08-28 (scope ruling + R1/R2/R3 + settlement):** Jack RULED the Jack-MLB sub-division copies **moneyline
+ totals + spreads** — everything Kalshi-copyable (WIDER than legacy). This **SUPERSEDES #8** and **PROMOTES the
totals/spread settlement question to a HARD R7 GATE** (§9). **R1 (migration 010), R2 (3-dim matcher + `db.SCHEMA_HEAD`),
and R3 (read-only `/live` list, tile-on-create) are all BUILT + box-scratch GREEN (§13–§15).** ① `KXMLBSPREAD` exists;
spread encoding + type discriminator established from live data (§14). **Settlement (§15d): moneyline is CLEAR
(BASEBALLGAMEWIN = official result = Polymarket); K5 REFRAMED (Kalshi settles at last-fair-price, never voids); MLB
TOTALS/SPREAD contract-terms are the remaining R7 gate.** **NOT deployed — LIVE stays schema 9; R2 pure/inert, R3
activates on a pm_web restart only.** **R4 and above remain UNAUTHORIZED.** No deploy/restart/live-DB-write. Branch
`prediction-markets-stage3-2026-08-28` (off `2d9a700`; `origin/prod-live 7220e32`; overnight `980fe53`). **[BOX-ONLY]**
marks a fact that needs the box.

Stage 3 is the MONEY LAYER — the first stage where a defect costs dollars. Jack rules; this scopes. Build one rung,
prove on box-scratch, HALT between rungs, never chain.

---

## 0. Terminology (load-bearing — do not trip on it)
`PM_REBUILD_PLAN §C` calls "Stage 3" the **farm-level actions only** and calls the **live/money layer "P3."** Jack's
task uses "Stage 3" for the **whole span** (farm actions + the live money layer, `PM_REBUILD_PLAN §C:376`). This plan
decomposes the whole span.

---

## 1. The four scoping questions (a–d) — answered

### a. ★ Which whales does legacy `poly_kalshi_mlb` copy? — **PULLED (R0, 2026-08-28)**
**Exactly TWO whales, both already pinned + active in `mlb` on the new platform** (full detail §12):
`SDTrading` (`0x16bb9951a36fce71e2ef57890b786145e0ba8492`) and `xifutloong3`
(`0x2dc13c6bda81b202281e796953a7323de675b33c`). **No roster gap** — the parallel test can copy the same two whales
as-is. Finding for Jack (§12): the count is 2 (small/clean, not impractical), and SDTrading's edge is totals-weighted,
so a moneyline-only first-live (ruling #8) copies a thinner slice of it.

### b. Legacy credentials + order placement
`KALSHI_KAREN_*` creds via `utils/secrets.py` (KeyVault + managed identity; PEM → transient `/tmp` file). Its own
executor `agents/strategies/poly_kalshi_executor.py` → `KalshiLiveBroker.place_order` (`brokers/kalshi_live.py`), with
8 guardrails, fail-closed, dry-run gating, `audit_event` journaling. **It bypasses the engine's `RiskAgent` by design.
Out of scope to edit — it is the SPEC for Stage 3's central chokepoint, not code to extend.**

### c. Kalshi order API — verified against BOTH the proven in-repo client AND the live docs
`POST /portfolio/events/orders` (V2; v1 removed → 410). Required: `ticker`, `side` (`bid`=buy YES / `ask`=sell YES),
`count` (string), `price` (2–4-dec dollar string), `time_in_force` (IOC/FOK/GTC), `self_trade_prevention_type`.
Optional: `client_order_id` (idempotency), `reduce_only` (exits), `post_only`, `expiration_time`, `subaccount`,
`order_group_id`. Auth headers `KALSHI-ACCESS-KEY/-SIGNATURE(RSA-PSS)/-TIMESTAMP(ms)`. Cancel:
`DELETE /portfolio/events/orders/{id}`. Fills from the create response. Hardened NO-leg `1−yes_price` conversion (the
"$163.84 bug", `kalshi_live.py:196-211`); benign `KalshiNoFill` vs loud `OrderPlacementError`; 2¢ slippage cap.

### d. Credentials + target account
`utils/secrets.py` wires both `KALSHI_API_KEY_ID/PEM` and `KALSHI_KAREN_API_KEY_ID/PEM`. **Ruling #5: the target
account is the existing `KALSHI` (original) account — no new account/key to provision.** Its 5-place wiring is
**CONFIRMED COMPLETE from code** (§12b). Two real accounts exist: `KALSHI` (original, distinct, cash $165.39) and
`KALSHI-KAREN`=`Karen-TWO` (same account, $493.51) — `KALSHI_403_SCOPE_2026-08-24.md`.

---

## 2. Reusable assets (reuse, do NOT rebuild) — cited
| Asset | Path | Role |
|---|---|---|
| Kalshi V2 order primitive | `brokers/kalshi_live.py` | The ONE execution call the central engine wraps (proven, hardened). |
| Legacy executor (guardrails) | `agents/strategies/poly_kalshi_executor.py` | SPEC for the 8 gates the chokepoint consolidates. Out of scope to edit. |
| MLB matcher (moneyline) | `data/mlb_poly_kalshi_match.py` | Extend to `(game, line)` for totals (R2). |
| Migration discipline | `prediction_markets/db.py` | `MIGRATION_010` next; PURE DDL; config in code; bump `schema_version` post-commit. |
| Status lifecycle | `paper.py`, `farm.py` | `candidate`/`pinned` + `active`; demote preserves paper (§F-5 structural). |
| Web route pattern | `prediction_markets/web/app.py`, `pm_dashboard.html` | Loader-off-thread → TemplateResponse; LIVE card disabled today. |
| Credential wiring | `utils/secrets.py` | 5-place pattern (already complete for KALSHI). |
| Signal seam | `paper.poll_pinned` | The `/positions` read = the live-copy signal source. |
| Sold-vs-redeemed signal | `polymarket_data_api_client.py` `ActivityRow` | side BUY/SELL + type — the only sold-vs-redeemed discriminator (D1). |

**No central Kalshi chokepoint exists today** (legacy runs its own). Stage 3's is net-new and must NOT re-introduce
the `RiskAgent` freeze (a synchronous ~1.19M-row `audit_event` scan froze the loop, `agents/risk.py:290-296`). **Caps
= O(1) in-memory counters, journal-backed for restart, never a scan on the order path.**

---

## 3. The rung ladder — money gate LAST (rulings threaded in)
Standing discipline per rung: build → box-scratch green → per-rung explicit authorization → deploy → verify → HALT.
`origin/prod-live` FF-only; `95e78c4` reachable. R0–R6 pure-additive/safe. **Money gate = R7.**

> **Activation rule (state on every deploy):** pm_web restart activates read-only UI + POST routes only; the migration
> runs on the next `init_db()`; the **execution engine (R4/R5/R7/R8) activates only when the engine/scheduler process
> runs it**, never via a pm_web restart.

- **R0 — PREP (no deploy): DONE (§12).** Whale list pulled; totals contract confirmed; wiring confirmed; balance from
  record. Only remaining R0-class item is the totals settlement rulebook (deferred, §9 #6).
- **R1 — Schema `MIGRATION_010` (PURE DDL):** `pm_account` (credential ref + **NULLABLE `owner_identity`**; NO
  `pm_user`/`role`/`grant`); sub-division `(account, category)` + config columns — **sizing basis = FIXED for the first
  sub-division (ruling #1); keep the column shape able to carry Kelly later, do not build Kelly**; per-sub-division
  live-order log table. Activation: `init_db()`. Proof: 9→10 idempotent on a copied DB. Stop: any table rebuild;
  non-atomic `schema_version`.
- **R2 — MLB matcher + TOTALS (pure functions):** `(game, line)` strike index + copy-only-when-exact-strike-exists-
  AND-liquid (skip, never round the far-tail). **Ruling #8: R2 is still BUILT (cheap, inert), but totals do NOT go
  live in the first period.** Depends on the R0 totals gate (contract confirmed exists, §12c). Spreads dropped.
- **R3 — Target-account creds (already wired) + read-only LIVE list / tiles:** enable the disabled dashboard card;
  `GET /live` + `GET /live/{account}/{category}` mirroring the farm route pattern; read-only. **Ruling #4: a
  sub-division tile appears ON CREATE, not on first trade — design the honest-empty state for a created-but-not-yet-
  trading sub-division.** Activation: pm_web restart. Stop: any tile renders fabricated live data.
- **R4 — Central engine in DRY-RUN (single chokepoint, no placement):** signal (`poll_pinned` `/positions`) →
  sizing/risk (**fixed amount, ruling #1**) → single chokepoint → per-sub-division log; terminal step logs the exact V2
  body but does NOT call `place_order`. Whale-exit = **Option D hybrid (ruling #2, §7)**. Activation: engine process
  (NOT pm_web). Stop: two sub-divisions → two execution paths; any path reaches `place_order` before R7; NO-leg
  inversion regresses.
- **R5 — Arm / kill-switch (money gate mechanism; not live):** per-sub-division + global armed/disarmed via the
  persistent `agent_state` halt; engine may reach `place_order` only when armed. **Ruling #3: default = fail-safe
  DISARMED on restart; conditional auto-arm stays off the table until boot-reconcile exists.** Stop: default anything
  but OFF; kill doesn't halt in-flight; flag flippable without authorization.
- **R6 — Three farm actions (POST routes):** Promote-to-watchlist (candidate→pinned + seed paper; **needs Stage 4
  prospects — ships but inert in prod until Search**); Demote (pinned→candidate, **paper PRESERVED §F-5**);
  Promote-to-LIVE (attach a pinned pair to an `(account, category)` sub-division joined ON CATEGORY — does not itself
  place an order). Activation: pm_web restart. Stop: demote deletes/orphans paper; Promote-to-LIVE fires without R5.
- **R7 — ★ FIRST LIVE ORDER (THE MONEY GATE):** with R5 armed for one sub-division, smallest size (1 contract), on
  the `KALSHI` account (ruling #5). **Ruling #8: MONEYLINE ONLY for first-live.** Go-live gate §8 must pass first
  (incl. a proven successful POST — no live order has ever succeeded on any account). Activation: engine armed (NOT
  pm_web). Stop: preflight fails; size >1; wrong-leg price; idempotency fails; any other sub-division armed.
- **R8 — Parallel-test observation:** Jack-MLB copies the SAME 2 legacy whales (§12), side-by-side with legacy (which
  stays running, unedited). Observation-only. Stop: inexplicable divergence; chokepoint bypass; size creep.

**Safety class:** R0 decision · R1 pure-additive · R2 pure-additive · R3 read-only UI · R4 dry-run · R5 mechanism
(default OFF) · R6 additive writes (inert until armed) · **R7 REAL MONEY** · R8 observation.

**Stage-4 dependency:** only R6's Promote-to-watchlist needs Search prospects; the money path (R4/R5/R7/R8) and the
parallel test copy already-pinned legacy whales and are NOT blocked by Stage 4.

---

## 4. Central risk chokepoint — design
One PM-wide chokepoint every live order transits; sub-divisions produce signal+sizing intent and never call the
broker. Fixed gate order (a reject consumes no budget, burns no idempotency key): (1) **disarm check FIRST** (global
then per-sub-division; read error → disarmed); (2) **per-order notional USD cap** (not contract count); (3)
**strike-exists-exact + liquid** (never nearest; re-checked at exit); (4) **dedupe/idempotency** — deterministic UUID5
over `(sub_division, wallet, ticker, outcome, stable_signal_id)`, checked against the **durable journal** (survives
restart), Kalshi `client_order_id` as backstop; (5) **per-sub-division daily cap** (in-memory `_deployed_usd`,
UTC-reset, journal-backed — **never an audit scan**); (6) **per-account exposure cap** (anchor to real balances:
`KALSHI` $165 / `KALSHI-KAREN` $493); (7) **slippage** (within `max_slippage_cents`; fail-closed if live + no quote);
(8) **count ceiling** (trip the persistent halt — the real-time backstop the loss-halt can't be, since Kalshi P&L is
only known at settlement). One chokepoint beats per-sub-division checks because aggregate caps are only definable
where all sub-divisions are visible, there is one dedupe namespace + one kill path, and it contains the first-order
blast radius to one reviewable path. It wraps `kalshi_live.place_order` verbatim (NO-leg inversion, idempotency,
benign-nofill split, $0-preflight already there); its job is to hand that primitive gate-approved inputs.

---

## 5. Kill-switch / arming — design (default DECIDED: fail-safe disarmed)
Reuse the persistent `agent_state` halt row (`StrategyState.persist_halt`). A single operator action sets the
persisted disarm; the chokepoint reads it first, so the next order and all after (across restarts) are blocked. Expose
via the LIVE page (R3) AND a CLI/direct write (works if pm_web is down). **Default on restart = DISARMED (ruling #3):**
a restart is exactly when in-memory counters/high-water marks are gone; auto-resuming risks a reset daily cap or
copying history on boot; disarmed forces a human to confirm boot-reconciliation before re-arming. **Accepted cost:** an
unattended restart leaves the live lane down until Jack re-arms. Conditional auto-arm stays off the table until
boot-reconcile exists. Scope: per-sub-division + a global master kill. Auto-disarm (latching): loss threshold (fires
late), consecutive loud `OrderPlacementError`, auth failure, count ceiling, boot-reconcile mismatch. Residual: a kill
after the halt-check but before `post()` is one order wide.

---

## 6. Risk table — what can lose money, the control, the cost
| # | Failure mode | Trigger | Blast radius | Control | Cost | Residual |
|---|---|---|---|---|---|---|
| K1 | **Phantom exit from page-cap** | small leg falls off default `/positions` page, or `fetch_positions_book` false-`complete` → "gone" → real SELL | dumps a real position on a data artifact, repeatable | exit from `/activity` SELL not `/positions` disappearance; confirm-vanish over N complete-book polls; stricter exit-completeness gate | +N×poll exit latency; two-source reconciliation | an un-pageable tiny exit is missed (stuck in a losing copy) — preferred over dumping |
| K2 | **Sold-vs-redeemed** | leg leaves book via redemption not sell | exit something that would settle / sell a winner early | `/activity` `type=TRADE,side=SELL` vs `REDEEM`; cross-check settled-bound | one `/activity` fetch; correlation logic | activity/positions lag can disagree → delayed exit |
| K3 | **Detection latency / stale book** | 10s–2min poll gap | enter/exit worse than whale; IOC/FOK 0-fill | 2¢ slippage cap; `reduce_only` exits; fail-closed no-quote | more 0-fill skips | both legs can miss by >2¢ → skip or overpay; do not raise the cap silently |
| K4 | **Wrong/illiquid strike (totals)** | Poly line with no Kalshi strike, or thin strike | different wager, or stuck unable to exit | exact-strike-only (skip, never round) + liquidity floor at entry AND exit | index+quote check; skips off-ladder (~0.6%) | liquid-at-entry/thin-at-exit → re-check at exit |
| K5 | **Settlement divergence (totals/spreads)** — REFRAMED 2026-08-28 | shortened/called/canceled game: **Kalshi settles at "last fair market price"** (NEVER void/stake-back — confirmed across the NBA/NFL sibling contracts) which Polymarket may not recognize | a copied total/spread resolves at a Kalshi traded price ≠ Polymarket's outcome (not "one voids") | resolve the MLB **TOTALS + SPREAD** contract-terms before those go live (§15d); **moneyline is CLEAR** (BASEBALLGAMEWIN → official result = Polymarket's rule) | R7 gate | shortened-game tail; the NBA "totals already-reached settle on available strikes" carve-out is a HYPOTHESIS to verify for MLB, not a fact |
| K6 | **Double order / idempotency** | same signal twice (retry/restart) | 2× exposure+cost | UUID5 over WALLET; **stable signal_id** (tx_hash); durable-journal dedupe + Kalshi backstop | none if signal_id stable | in-memory-only dedupe dies on restart |
| K7 | **Sizing over-bet** | wrong base_price / contracts explode | 10–100× in one order | per-order USD cap (notional); `usd_to_contracts` floor+min1; primitive validates `0<price<1` | none | valid-but-wrong price passes range check → USD cap must catch dollars |
| K8 | **Auth/geo outage mid-position** | order-path 401/403 or rotation | can't ENTER, or worse can't EXIT | root-cause before go-live (§8); preflight; auth-failure → auto-disarm + flag open positions; manual flatten | disarmed while down | auth dies with positions open → human exits on Kalshi |
| K9 | **Engine crash w/ open positions** | OOM/deploy/panic | unmanaged positions; lost counters → replay/double-count | durable journal state; cold-start seeds HWM without emitting; **fail-safe disarmed (ruling #3)**; boot-reconcile | restart downtime | crash-window positions unmanaged regardless |
| K10 | **Kill-switch failure** | kill doesn't persist/propagate | the stop control fails | persist halt; check first; global+per-sub-division; auto-disarm | one `agent_state` read/submit | in-flight window is one order wide |

Anchors: legacy ~ $5/trade, $20/day, 25 orders/day; account balances $165 / $493; a ~$50 per-account open cap is
appropriate for first-live.

---

## 7. Whale-exit (D1) — DECIDED: Option D, the hybrid (ruling #2)
**Ruling #2 (Jack):** exit signal = **`/activity` SELL as the trigger + `/positions` size reduction as confirmation.**
Reasoning (recorded): **bias-down, never up** — a missed exit costs one losing copy; a phantom exit dumps a real
position and can repeat. D is the phantom-averse choice and the only option that handles partials AND sold-vs-redeemed
together. **Accepted costs:** two pollers, two rate budgets, slowest. **Residual (recorded honestly):** if the two
signals never co-occur within the window, the exit is missed. The paper lane deliberately does NOT solve the live
exit (it defers vanished legs to gamma adjudication days later, `paper.py:356`) — so this is net-new for the LIVE lane.

*Options considered (kept for the record):* A `/positions` paging+confirm-vanish (blind to sold-vs-redeemed; phantom-
prone via K1); B `/activity` SELL only (best semantics, can miss/lag; needs partial handling); C size-delta (handles
partials, most noise-sensitive); **D hybrid (CHOSEN)** — highest confidence per fired order (K1 needs BOTH signals
wrong at once), best sold-vs-redeemed handling, at the cost of latency/complexity/coverage.

---

## 8. Go-live gate — minimum proof before the first real order (R7)
1. **A successful order POST is demonstrated** — demo first, then one 1-contract real order on the `KALSHI` account.
   (No live POST has ever succeeded; the one prior exercise 401'd on a *stale key* on the *legacy* division, likely
   cleared by the 08-27 reboot but UNVERIFIED, and never on the target account. This gate is correct regardless.)
2. **Dry-run parity on the box** — the built V2 body is byte-correct for real whale signals (incl. a NO-leg case:
   prove the `1−P` inversion holds *through the new engine*). (Moneyline only for first-live, ruling #8.)
3. **One smallest-size real order on `KALSHI`, manually observed** — places, fills or benign-nofills, FillEvent matches.
4. **Reconcile that fill against Kalshi's portfolio/balance delta** — esp. the per-contract-vs-total fee convention
   (`kalshi_live.py:188`). Exercises boot-reconcile once by hand.
5. **Kill-switch proven** — arm, kill mid-session, next order blocked, halt survives restart; one auto-disarm fires.
6. **Idempotency across restart** — same signal, kill+restart, no double order.

---

## 9. RULINGS — DECIDED (Jack, 2026-08-28)
1. **#5 Target account — the existing `KALSHI` (original) account is Jack's.** Secrets `KALSHI_API_KEY_ID` +
   `KALSHI_PRIVATE_KEY_PEM`, already in vault + wired. No new account/key. **Wiring CONFIRMED complete (§12b).**
2. **#2 Whale-exit (D1) — Option D (hybrid):** `/activity` SELL trigger + `/positions` size-reduction confirmation.
   Bias-down; accepted costs (two pollers, slowest); residual = missed exit if signals never co-occur in-window (§7).
3. **#3 Kill-switch default — fail-safe DISARMED on restart.** Conditional auto-arm off the table until boot-reconcile
   exists (§5).
4. **#1 Sizing — FIXED AMOUNT for the first sub-division.** Kelly needs a reliable edge estimate; the paper lane has
   n≈2 closed. Keep the column shape able to carry Kelly later; do not build it (R1/R4).
5. **#8 First-live scope — SUPERSEDED 2026-08-28.** Was "moneyline only." Jack's SCOPE RULING now governs: the
   Jack-MLB sub-division copies **moneyline + totals + spreads** — everything Kalshi-copyable, deliberately WIDER than
   legacy (which copies win/loss only; the point is to be *better*, not a like-for-like replica). ① confirms all three
   are listed (`KXMLBGAME`/`KXMLBTOTAL`/`KXMLBSPREAD`, §13). R2 builds all three; first-live is not moneyline-restricted.
6. **#4 Tile visibility — a sub-division tile appears ON CREATE, not on first trade** (R3 honest-empty state).
7. **★ SCOPE RULING (Jack, 2026-08-28): the sub-division is WIDER than legacy BY DESIGN.** ③ **R8 is therefore NOT
   an apples-to-apples comparison** — a divergence from legacy is EXPECTED by design, not a defect; a later reader must
   not read a difference as the platform underperforming. ④ **Copying only the 2 legacy whales is a PARALLEL-TEST
   CONSTRAINT, not a permanent property** — the 8 other mlb-pinned whales are excluded only to keep the comparison
   clean; once R8 concludes they are considered on their own merits.

**Open / gated:**
- **#6 Totals (& spread) settlement — PROMOTED 2026-08-28 to a HARD R7 GATE** (was deferred; the scope ruling put
  totals/spreads back on the first-live path). Whether Kalshi and Polymarket agree on rain-shortened / called-official
  / canceled games must be resolved BEFORE R7. The API `rules_primary` gives the base rule but NOT shortened-game
  handling (that lives in the contract-terms PDF; general sportsbook convention voids totals unless 9 innings, which
  DIVERGES from Polymarket's official-score rule). **Cost (§13d):** one or two authenticated reads of the live market's
  contract-terms link, or a direct Kalshi rulebook read — a Jack/ops verification, not a code task.
- **#7 Owner-filtering (P3)** — see-only-own vs see-all-with-own-highlighted; `P2_PLAN §11` says do not decide now.

---

## 10. Ranked — next steps (R0 + R1 + R2 + R3 DONE; R4 pending Jack's authorization)
1. **R0 — DONE (§12):** whale list pulled (2 whales, both pinned+active in mlb — no gap); ruling-#5 wiring confirmed.
2. **R1 — schema BUILT + box-scratch GREEN (§13). NOT deployed — LIVE remains schema 9** (build-only; live `init_db`
   9→10 is a SEPARATE authorization). Nothing armed or money-moving.
3. **R2 — 3-dimension matcher + `db.SCHEMA_HEAD` BUILT + box-scratch GREEN (§14).** Pure/inert; LIVE untouched.
4. **R3 — read-only LIVE list (`/live`, tile-on-create) BUILT + box-scratch GREEN (§15).** pm_web-only; activation =
   a pm_web restart; defensive against the money-layer tables being absent (honest-empty at schema 9). LIVE untouched.
5. Then R4 (dry-run engine, fixed sizing, Option-D exit, imports the R2 matcher) → R5 (arm/kill, default disarmed) →
   R6 (farm actions) → R7 (first live order, `KALSHI`, §8 gate) → R8 (parallel test, the 2 legacy whales). One rung
   per authorization; never chain.
6. **Resolve the MLB TOTALS + SPREAD settlement contract-terms BEFORE those go live** — a hard R7 gate (§15d); the
   moneyline settlement is CLEAR (§15d).

---

## 11. Honest limits
- The current order-path 401 state and the totals settlement rulebook are **[BOX-ONLY]** / not cheaply retrievable;
  each is placed as an explicit gate (§8, §9 #6), not assumed.
- **A fresh KALSHI balance was intentionally NOT read.** It is an authenticated vault-cred call (no unauthenticated
  balance endpoint); doing it here (dumping the engine `/proc` env for `KEY_VAULT_URI` + an authenticated broker read)
  would cross the read-only/never-touch-secrets boundary — and was correctly blocked. Reported figure is the
  2026-08-24 verified value ($165.39 cash / $314.20 portfolio; account `updated_on 2026-05-10`, unchanged). A fresh
  read is a separate, explicitly-authorized step if wanted.
- The 08-24 "401 blocker" was a *stale-key* finding on the *legacy* division, likely cleared by the 08-27 reboot — NOT
  asserted as a current blocker; converted into the §8 go-live gate (prove a successful POST), correct regardless.

---

## 12. R0 EXECUTION FINDINGS (2026-08-28, read-only; runner `cc\pm_stage3_r0_ro.*`)

### 12a. ★ Legacy `poly_kalshi_mlb` whale roster — the finding
Pulled read-only from `data/trading_corp.db` `agent_state(agent='poly_kalshi_mlb', key='live_whales')` (mode=ro,
replicating `rosters.read_agent_state`'s exact query) and cross-referenced to `pm_watchlist` `(wallet, mlb)`:

| wallet | name | new-platform (wallet, mlb) |
|---|---|---|
| `0x16bb9951a36fce71e2ef57890b786145e0ba8492` | SDTrading | status=pinned, active=1 |
| `0x2dc13c6bda81b202281e796953a7323de675b33c` | xifutloong3 | status=pinned, active=1 |

- **COUNT = 2.** Gap analysis: **NONE** — both legacy whales are pinned+active in `mlb` on the new platform, so the
  parallel test can copy the same whales with no roster change.
- **Finding for Jack (not designed around):** (1) the legacy division copies only **2** whales — the parallel test is
  therefore small and clean, *not* impractical; the Jack-MLB sub-division should copy **only these two** (the new
  platform has **8 other** mlb-pinned+active whales NOT in the legacy roster — copying them would break the
  "same whales" comparison). (2) **SDTrading's edge is totals-weighted** (`MLB_TOTALS_COPYABILITY`: ~53% moneyline-only
  vs ~91% with totals), so a moneyline-only first-live (ruling #8) exercises a **thinner slice** of SDTrading's
  signals — fine for path-proving, honest that it won't cover its main edge. (SDTrading is also the whale that booked
  the −236.91 `mlb` paper-loss cluster overnight — the paper lane is already surfacing its real record.)

### 12b. Ruling #5 target-account (`KALSHI`) wiring — CONFIRMED COMPLETE (from code)
`utils/secrets.py`: `KALSHI_API_KEY_ID`/`KALSHI_PRIVATE_KEY_PEM` present in `_SECRET_KEY_NAMES` (54,59), the `Secrets`
dataclass (178,185), `_populate_from_keyvault` expected list (309,310), `load_secrets` (404,406), redaction (437), and
`assert_live_ready` has its branch (509-510). **Nothing to provision.** Vault-resolution + a fresh balance were NOT
read here (§11). Balance of record: **$165.39 cash / $314.20 portfolio** (2026-08-24; account unchanged since
2026-05-10).

### 12c. Totals contract (`KXMLBTOTAL`) — EXISTS
Unauthenticated public read returned live markets (e.g. `KXMLBTOTAL-26AUG281915SEATOR-9` "Over 8.5 runs scored";
`-8` "Over 7.5"). The series exists → **R2's totals matcher is buildable** when authorized. `rules_primary` gives the
base "score more than N runs" rule but **not** shortened/called/canceled handling → the settlement question is now a
**HARD R7 GATE** (the scope ruling superseded #8; see §13d), not deferred.

---

## 13. R1 EXECUTION + SCOPE-CHECK FINDINGS (2026-08-28; runner `cc\pm_stage3_r1_boxscratch.*`, board-authorized)

### 13a. Migration 010 (money-layer schema) — BUILT + box-scratch GREEN
Committed on this branch (schema `1ee0d63`; test head-pins `cf141be`). `MIGRATION_010` (PURE DDL, numbered on landing)
adds three tables; arming reuses the persistent `agent_state` halt (no arming table):
- **`pm_account`** — `account_id` PK, `secret_ref` (credential REFERENCE, never a value), **NULLABLE `owner_identity`**
  (Authelia login→account mapping), `venue`, `active`. NO `pm_user`/`pm_role`/`pm_grant` (test asserts them absent).
- **`pm_subdivision`** — PK `(account_id, category)`; **FIXED sizing** (`sizing_mode` default `'fixed'`; `fixed_stake_usd`;
  `kelly_fraction` carried-not-built); risk caps (`per_order_usd_cap`/`daily_usd_cap`/`max_open_usd`/`max_orders_per_day`/
  `max_slippage_cents`; DDL-NULL = fall back to code CONFIG_DEFAULTS, no config WRITE); **`market_types` default
  `'moneyline,total,spread'`** — one text list expresses any subset/superset without a future migration.
- **`pm_subdivision_order`** — the per-sub-division live-order journal, shaped to `brokers/kalshi_live.py`'s ACTUAL
  returns: `client_order_id` (idempotency), the submitted V2 body (`ticker`/`order_side`/`outcome_leg`/`is_exit`/
  `submitted_count`/`submitted_price`/`time_in_force`), the outcome (`outcome_status` ∈ filled|no_fill|rejected|error),
  and fill facts (`broker_order_id`/`fill_count`/`fill_price`[outcome-leg, 1−yes for 'no']/`remaining_count`/`fee`) +
  `dry_run`.

**Box-scratch proof (read-only; LIVE untouched):** `test_db.py` 12/12 + **FULL PM suite all pass**
(`-p no:pytest_ethereum`). Migration 9→10 on a `.backup` copy of LIVE prod: schema 9→10; 3 new tables present +
**EMPTY**; **existing row counts UNCHANGED** (`pm_watchlist` 114 / `pm_paper_trade` 136 / `pm_paper_category_stats` 9);
**`pm_paper_config` 3→3 = PURE DDL, no config row** (the 009 property); **no table dropped (no rebuild)**; idempotent
re-run. **LIVE stayed schema 9; engine 676 / pm_web 42343 PIDs unchanged before+after.** Box-scratch also caught 6
stale schema-head-pins in other test files (`test_caveat_analytics`/`test_names`/`test_removal_gate`/
`test_stage1_paper_rollup`/`test_web_healthz`) → bumped 9→10 (`cf141be`); the recurring per-migration churn is why the
**SCHEMA_HEAD-constant proposal** (already flagged in `test_removal_gate`) stays open as housekeeping.

### 13b. ★ ① Spread series EXISTS — the "spreads dropped" evidence was mis-probed
Unauthenticated live catalog read (188 MLB Sports series): **`KXMLBSPREAD` "Pro Baseball Spread" — 2 open markets**
(also `KXMLBF5SPREAD` "First 5 Innings Spread", `KXWBCSPREAD`). `KXMLBRUNLINE` → 0. The recorded rationale ("Kalshi
lists no MLB run line → spreads DROPPED") probed the *run-line* naming; Kalshi lists the game spread under
**`KXMLBSPREAD`**. **So spreads ARE copyable** — under Jack's scope ruling, R2 must build a spread dimension too (not
just moneyline+totals). Format needs a look at R2 (likely a ±run strike like totals; the exact-strike-exists-and-liquid
guard applies identically). **Surfaced as a finding, not designed around.**

### 13c. Which MLB market types the sub-division config expresses (from the code)
The matcher (`mlb_poly_kalshi_match.py`) gates on the Poly slug suffix: **moneyline** (no suffix), **total**
(`-total-{N}pt{M}`), **spread** (`-spread-…`), prop (excluded). Given the scope ruling and ① (all three are listed on
Kalshi — `KXMLBGAME`/`KXMLBTOTAL`/`KXMLBSPREAD`), the config must express **{moneyline, total, spread}**.
`pm_subdivision.market_types` (default `'moneyline,total,spread'`) carries any subset/superset in ONE column, so a later
change needs no migration — including carrying `'spread'` even if a specific spread market is transiently unlisted
(R2 skips unlisted at runtime).

### 13d. ② Totals (& spread) settlement — now a HARD R7 GATE; cost to resolve
The scope ruling puts totals (and spreads) on the first-live path, so the settlement-divergence question (Kalshi vs
Polymarket on rain-shortened / called-official / canceled games) is **no longer deferred — it is a hard R7 gate**
(§8, §9 #6). **Cost:** the market `rules_primary` (cheap, unauthenticated) does NOT carry shortened-game handling; the
full rule lives in the Kalshi contract-terms PDF (not in the API object; a web fetch 404'd on the guessed name). So
resolving it is one-to-two authenticated reads of the live market's contract-terms link, or a direct rulebook read — a
Jack/ops verification, not a code task. General sportsbook convention (totals void unless 9 innings) *diverges* from
Polymarket's official-score rule, so this is a real gate, not a formality.

---

## 14. R2 EXECUTION FINDINGS (2026-08-28; data-gather `cc\pm_stage3_r2_datagather_ro.*`, box-scratch `cc\pm_stage3_r2_boxscratch.*`)

### 14a. Three-dimension matcher + `db.SCHEMA_HEAD` — BUILT + box-scratch GREEN (code `2e07b3f`)
Pure functions in `data/mlb_poly_kalshi_match.py`, INERT (no caller until R4). Moneyline (`match_poly_to_kalshi`)
kept **byte-identical** — the legacy `poly_kalshi` copy-trader consumes it. Added, established FROM LIVE DATA:
- **Totals** — Poly `-total-{W}pt{F}` → strike `W.F`; Kalshi `KXMLBTOTAL-{stem}-{N}`, strike `N−0.5`, **Over→YES /
  Under→NO** (same-ticker NO leg).
- **Spreads** — Poly `-spread-{home|away}-{L}` (all 161 observed = `1pt5`); the `{home|away}` token is the **anchor**
  (the −L side), outcome names the team bet. Kalshi `KXMLBSPREAD-{stem}-{TEAM}{N}`, strike `N−0.5`, YES = "{TEAM}
  wins by over strike". **outcome==anchor → `{anchor}{N}` YES; outcome==other → `{anchor}{N}` NO** (complement, no
  push at a half-run). The Kalshi **leg is carried on `MatchResult`** so the executor never re-derives it
  (the $163.84-class guard).
- **`stem`** (`{YYMONDD}{HHMM}{TEAMBLOB}[G{n}]`) is shared verbatim across the three series → totals/spreads join to
  the game via the moneyline index, no blob re-splitting.
- **Guards:** EXACT-STRIKE-ONLY (missing strike → labelled `no_kalshi_strike`, **never rounded** — the far-tail case);
  `market_types` config filter (a type not in the sub-division → `skip_market_type_excluded`, not an error);
  `liquidity_ok(market, …)` pure K4 gate (documented **must re-check at exit**).
- **`db.SCHEMA_HEAD`** — the earned constant (`max(v for v,_ in MIGRATIONS)`); the 6 is-at-head checks now reference
  it, so a future migration bumps ONE place. Migration-SPECIFIC guards (e.g. the migration-008 partial-DB `==8`) stay
  explicit. A `test_schema_head_tracks_migrations` guarantees the wiring.

**Box-scratch (read-only; LIVE untouched):** new matcher tests **16/16**; `test_db.py` **13/13** (incl. SCHEMA_HEAD);
**FULL PM suite all pass** (SCHEMA_HEAD rewiring across 6 files); **★ legacy copy-trader regression 35/35** (moneyline
not regressed). **LIVE stayed schema 9; engine 676 / pm_web 42343 PIDs unchanged.** R2 adds no migration.

### 14b. ★ Type discriminator — ANSWERED from real slugs (5,014 ML / 416 total / 161 spread / 92 prop)
The slug suffix after `mlb-{a}-{h}-{date}` is authoritative: **no suffix → moneyline**; `-total-…` → total;
`-spread-…` → spread; **any other suffix (`-nrfi`, `-349`, …) → prop**; a slug that isn't `mlb-{a}-{h}-{date}` →
`non_mlb`/`mlb_non_game`. **An unrecognized suffix or non-game slug is NEVER silently moneyline — it is a labelled
skip** (asserted in tests: `mlb-…-349` and `mlb-…-nrfi` → prop→`skip_non_ml`; `nba-…` → `skip_non_game`).

### 14c. ★ Spread series confirmed + real encoding (the ① mis-probe, closed)
`KXMLBSPREAD` "Pro Baseball Spread" lists per-team run-line markets (e.g. `…-TOR2`/`-TOR3`/`-TOR4` and `-SEA2/3/4` for
one game). Real Poly spreads (`mlb-cin-chc-…-spread-home-1pt5` outcome=Cubs → Cubs −1.5; `mlb-bos-tor-…-spread-away-1pt5`
outcome=Toronto → Toronto +1.5) map cleanly. Spreads are copyable and BUILT into R2.

### 14d. ② Settlement (R7 gate) — SOURCE LOCATED, extraction needs OCR/human (reported, not burned)
The Kalshi **series objects carry the authoritative `contract_url` PDFs** —
`assets.kalshi.com/regulatory/product-certifications/MLB{TOTAL,SPREAD,GAME}.pdf` (a **separate cert per market type**,
consistent with "the answer may differ by type") — plus `settlement_sources` (MLB for total/spread; ESPN/Fox/MLB for
game) and, on the game market, `rules_secondary` = "postponed/delayed → remain open, close after the rescheduled game."
**But the three PDFs are font-encoded and did not extract via WebFetch (no OCR available), so the shortened/called/
canceled rules are not machine-readable by this tooling.** **What it needs:** an OCR or human read of three ~200–320KB
PDFs (a Jack/ops verification, not a code task) BEFORE R7 turns totals/spreads live. General convention (totals void
unless 9 innings) still *diverges* from Polymarket's official-score rule — the gate stands.

### 14e. §H checkpoint (R2)
R2 touches **none of the three lists' data bases** — it is a pure market-mapping utility (Poly→Kalshi) plus a
schema-head constant. Prospect (completed) / Watchlist (paper) / Live (P3) bases are all untouched and stay separate.

---

## 15. R3 EXECUTION FINDINGS + settlement CORRECTION (2026-08-28; box-scratch `cc\pm_stage3_r3_boxscratch.*`)

### 15a. Read-only LIVE list — BUILT + box-scratch GREEN (code `24ae03a`, test-fix `10bfe1b`)
`/live` + `/live/{account}/{category}` (pm_web-only), the dashboard LIVE card + nav enabled, new `subdivision.py`
(PM-ONLY, defensive reads). Tile-on-CREATE honest-empty ("created, no live trades yet"); vocab "Live sub-divisions",
no internal-name leak; READ-ONLY (no POST route, no order/arm controls; pm_web imports no broker). Box-scratch:
`test_live_r3.py` **9/9** + **FULL PM suite all pass** (incl. `pm_web-imports-no-engine`, healthz, nav, farm). LIVE
schema 9; engine 676 / pm_web 42343 unchanged.

**DEFENSIVE (load-bearing):** `subdivision.*` tolerate `pm_account`/`pm_subdivision` being **absent at schema 9** →
honest-empty, never a 500 — so R3 deploys on a pm_web restart INDEPENDENT of the migration-010 deploy; tiles appear
once 010 is live AND a sub-division exists. **secrets.py NOT modified** (KALSHI wiring already complete); a test proves
`assert_live_ready` fails LOUD on a missing key (it gates ANTHROPIC first, then KALSHI).

**Deploy set (all PM-ONLY):** `web/app.py`, `subdivision.py`, `pm_dashboard.html`, `pm_shell.html`, `pm_live_list.html`,
`pm_live_subdivision.html`, `pm_live_404.html`. **Activation = a pm_web restart ONLY** (no engine, no migration, no
DB write). **SHARED-file note:** `trading_corp/utils/secrets.py` is SHARED and was **NOT** in this set; the box-scratch
re-verified box==branch for it (sha256 `bc3f8166…` matched — no concurrent drift).

### 15b. §H checkpoint (R3)
R3 renders the **Live list's STRUCTURE** (Account-Category tiles + config); the Live **data basis** (P3 live-trade
tables) is not built, so the list is honest-empty by construction. Prospect (completed) / Watchlist (paper) bases are
untouched. The three data bases stay separate; R3 adds data to none of them.

### 15c. ★ Settlement filename CORRECTION (the guessed-name trap, again)
The moneyline contract-terms doc is **`BASEBALLGAMEWIN.pdf`** (sport word), NOT `MLBGAME.pdf` — §14d's
`MLB{…,GAME}.pdf` was a mis-record of the same class as the `KXMLBRUNLINE` mis-probe. Corrected here + in memory. The
exact `contract_terms_url`s (pulled live 2026-08-28): totals `https://assets.kalshi.com/contract_terms/MLBTOTAL.pdf`,
spread `https://assets.kalshi.com/contract_terms/MLBSPREAD.pdf` (+ product certs `…/regulatory/product-certifications/
MLB{TOTAL,SPREAD}.pdf`). Jack can download these directly.

### 15d. Settlement — ESTABLISHED from the actual documents (Jack pulled the NBA/NFL siblings)
- **MLB MONEYLINE (BASEBALLGAMEWIN): CLEAR.** A game shortened/called/ended-before-regulation that the governing body
  declares OFFICIAL with a winner **settles on that official result — MATCHES Polymarket.** The rain-shortened moneyline
  divergence we feared does NOT exist. Other moneyline fallbacks (abandoned/cancelled/suspended-not-resumed-within-48h;
  postponed-not-started-within-48h; home/away reversed; forfeit-before-first-pitch; playoff-series-game-never-reached)
  settle at **last fair market price**; suspended-and-resumed-within-48h keeps the market open.
- **★ K5 REFRAMED:** the "totals void unless 9 innings" sportsbook convention **appears NOWHERE in Kalshi's contracts**
  (checked baseball moneyline, NBA totals, NBA spreads, football spreads) — the fallback is ALWAYS **"last fair market
  price as determined by the Exchange,"** never void/stake-back. So the risk is NOT "one venue voids while the other
  settles"; it is **"Kalshi settles at a traded price Polymarket does not recognize."**
- **HYPOTHESIS to verify (not fact):** NBA totals carve out "totals already reached resolve on the basis of available
  strikes" even on an abandoned game. If MLB totals mirror it, an OVER already surpassed settles rather than reverting
  to last price. **Verify against the MLB TOTALS contract; do not assume.**
- **STILL AN R7 GATE:** the MLB **TOTALS** and **SPREAD** contracts SPECIFICALLY (Jack's uploads are the NBA/NFL
  siblings, not baseball's). Get them from the §15c URLs (one read each) and read them before those markets go live.
  Moneyline is not gated by this.

---
*R0 + R1 (schema) + R2 (matcher) + R3 (read-only LIVE list) done + box-scratch GREEN. LIVE untouched (schema 9; no
deploy/migration/DB-write/restart/cadence run). R4 and above are NOT authorized. HALT.*

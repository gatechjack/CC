# Core orchestration
langgraph>=0.2.50
langgraph-checkpoint-sqlite>=2.0.0
langchain-anthropic>=0.3.0
langchain-core>=0.3.20
anthropic>=0.40.0

# Brokers / data
ccxt>=4.4.0
robin_stocks>=3.4.0
pyotp>=2.9.0                # TOTP MFA code generation for Robinhood login
yfinance>=0.2.50

# Comms
python-telegram-bot>=21.6

# Configuration / env
python-dotenv>=1.0.1
pyyaml>=6.0.2
pydantic>=2.5             # research firm (typed engagement specs + product schemas)

# Data analysis
pandas>=2.2.0
numpy>=1.26.0
pandas-market-calendars>=4.4.0   # NYSE schedule incl. half-days/holidays for 0-DTE deadline gates

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0

# Phase 3 — Fidelity browser automation
playwright>=1.48.0         # after pip install: run `playwright install chromium`

# Dashboard (legacy — kept during migration to FastAPI)
streamlit>=1.32.0
nest_asyncio>=1.6.0

# Web command center (FastAPI + HTMX + Jinja templates).
# Dark trading-desk dashboard hosted in-process alongside trading_corp.
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
jinja2>=3.1.4
python-multipart>=0.0.12   # form parsing for HTMX POSTs
markdown>=3.7              # render LLM analysis (markdown → HTML) on division drill-down

# TradingView supplemental signals (supplemental only — never on execution path)
# Requires ENABLE_TRADINGVIEW=1 env var to activate at runtime.
tvdatafeed                 # historical OHLCV bars via TV WebSocket
tradingview-ta             # indicator snapshots via TV screener API

# Kalshi (Phase K1+ — Prediction Markets group). MIT-licensed community SDK
# with both async + sync clients, RSA-PSS request signing, REST + WebSocket.
pykalshi>=1.0.6
